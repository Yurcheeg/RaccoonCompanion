
//it's used to get the name of the item and putting it into the store popup. also id i think.

/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class StoreItem : MonoBehaviour
{
    public string itemName;
    public int itemCost;
    private StorePopUp storePopUp;

    private void Start()
    {
        storePopUp = FindObjectOfType<StorePopUp>(true); //true includes inactive objects (popup window)
    }


    public void UpdatePopUp(StoreItem itemName)
    {
        storePopUp.UpdatePopUpText(itemName); //makes the text mention the item that you are planning on buying
    }
}*/

